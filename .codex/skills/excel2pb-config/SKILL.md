---
name: excel2pb-config
description: 设计、修改和校验 excel2pb 游戏配置 Excel 表。当用户新建或编辑 assets/xls 下的 .xlsx、配置表表头、protobuf 结构、道具/商店/技能数值、多语言文本，或需要添加和检查 pk、unique、fk、index、i18n、时间戳、枚举等约束时使用；即使用户未提及 excel2pb，只要任务涉及这些配置表也应使用。
---

# excel2pb 配置表

excel2pb 将 `assets/xls/` 工作簿导出为 protobuf 二进制和读取代码。先理解表结构和约束，再改数据；这样可在导出前发现会导致 panic 或产生错误数据的问题。

## 按任务加载资料

- 需要用代码读取、修改或创建 `.xlsx`，尤其是读写字段批注时，先读 [references/openpyxl.md](references/openpyxl.md)。只能用支持批注的库；不要用 pandas 写回工作簿。
- 新增表或字段时，使用“表结构”“类型”“批注”规则。
- 填多语言、数组或嵌套结构时，使用对应规则。
- 在导出前检查时，逐项执行“校验清单”，报告工作簿、sheet、字段和 Excel 行号（数据行号 = 数据下标 + 5）。

## 表结构

- 工作簿名是导出的 proto 名；可放在 `assets/xls/` 下一级子目录。`~` 开头的临时工作簿会跳过。
- sheet 名是 message 名，需为合法标识符（字母开头，后续为字母、数字或下划线）。`#` 开头的 sheet 是草稿或备注，不导出。
- 以 `<EnumName>_Enum` 命名的 sheet 是枚举表；其他 sheet 是数据 message。
- 数据表前四行固定且不可错位：第 1 行字段名，第 2 行类型，第 3 行过滤（`c`、`s`、`cs`），第 4 行说明；数据从第 5 行开始。

## 类型与主键

基础类型为 `int32`、`int64`、`float`、`double`、`bool`、`string`、`timestamp`、`i18n`，以及已定义的枚举名或其他 sheet 名（嵌套 message）。类型单元格可加：

- `pk int32`：主键；每张需要生成读取代码的数据表都应有主键。
- `unique string`：此列值唯一。
- `repeated Reward`：数组。

多个 `pk` 是复合主键：每一列可以重复，但整组值必须唯一。主键第 3 行必须包含要生成代码的端，通常使用 `cs`。

`timestamp` 填 `YYYY-MM-DD HH:MM:SS`，会按配置的 `TimeZone` 导出 Unix 秒。枚举字段可填枚举名称或整数。枚举表第一项必须为值 `0`，例如 `InvalidItemType = 0`。

## 批注 tag

只读取**第 1 行字段名单元格**的批注。多个 tag 以 `;` 分隔：

- `fk:Item.ID`：本字段每个值都必须存在于 `Item` sheet 的 `ID` 字段；`repeated` 数组逐项验证。
- `fk:Reward.ItemID=Item.ID`：验证嵌套 `Reward` 内的 `ItemID`。
- `index`：声明索引；可与外键组合，例如 `fk:Item.ID;index;`。

## i18n 与复杂值

- 要翻译的字段第 2 行写 `i18n`，数据中填写源文本而不是 key。导出会合并到 `assets/xls/I18N.xlsx`，并将 `Sheet_Field_主键值` 写入二进制。
- 含 i18n 字段的表必须有稳定的主键；不要把 i18n 字段本身设为主键。复合主键以 `_` 连接。
- `repeated` 值用 `;` 分隔，例如 `1;2;3`。
- 嵌套值内部字段按目标 sheet 列顺序使用 `|` 分隔，多项使用 `;`，例如 `1001|2;1002|5`。

## 导出前校验清单

先报告会中断导出的硬错误，再报告会让数据失真的问题：

1. 每张数据表有主键，且主键或复合主键组合不重复；`unique` 列没有重复值。
2. 类型可解析：整数、浮点、布尔值、时间戳格式都正确；枚举名/整数合法；引用的枚举和嵌套类型存在。
3. 含 i18n 的表有主键；主键不会被目标端过滤掉。
4. 每个 `fk` 指向存在的 sheet 和字段，且每条值（含数组、嵌套项）都可在目标列找到。
5. 嵌套值用 `|` 拆出的字段数与目标 sheet 一致。
6. 数据表四行表头正确；字段与 sheet 名合法；数据始于第 5 行。
7. 枚举 sheet 符合 `<名称>_Enum`，且首项值为 `0`。

对每个问题说明：工作簿、sheet、字段、Excel 行号、违反的规则和可执行的修复方式。修改后运行与改动范围相符的检查；完整导出时也检查 `protoc` 及插件是否真实生成输出。
